<session id="todos">
  <header>
    <h2>Editing '<%= @list[:name] %>'</h2>
    <ul>
      <li>
        # the form is a button that submits to the end point, which is "/lists/<%= params[:id] %>/destroy"
        <form class="delete" action="/lists/<%= params[:id] %>/destroy" method="post">
          <button type="submit" class="delete">Delete list</button>
        </form>
      </li>
    </ul>
  </header>
</session>

<form action="/lists/<%= params[:id] %>" method="post">
  <dl>
    <dt>
      <label for="list_name">Enter a new name for your list:</label>
    </dt>
    <dd>
      <input name="list_name" placeholder="List Name" type="text" value="<%= params[:list_name] || @list[:name] %>">
    </dd>
  </dl>
  <fieldset class="actions">
    <input type="submit" value="Save">
    <a href="/lists/<%= params[:id] %>">Cancel</a>
  </fieldset>
</form>